#include <iostream>
#include <map>
#include <string>
#include "dictionary.h"
using namespace std;
/*
int main()
{
	Dictionary dt;
	dt.AddItem(string("one"), string("一个"));
	dt.AddItem(string("two"), string("两个"));
	dt.AddItem(string("China"), string("中国"));

	cout << "字典中所有的词条如下：" << endl;
	dt.DisplayAll();
	cout << endl;

	cout << dt.Lookup(string("China")) << endl;

	dt.DelItem(string("one"));
	cout << "删除后字典的所有词条如下:" << endl;
	dt.DisplayAll();
	cout << endl;
	return 0;
}
*/